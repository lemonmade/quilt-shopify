query Products {
  collections(first: 10) {
    edges {
      node {
        id
        title
        handle
      }
    }
  }
  products(first: 10) {
    edges {
      node {
        id
        title
        handle
      }
    }
  }
}
